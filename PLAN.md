# v3 研究设计与冻结规则

## 样本

- Binance USDT 永续，UTC `[2026-03-01, 2026-09-01)`，最终 20 个标的，FIL 替代停牌后的 TON。
- 形成期 3—4 月，验证期 5—6 月，留出期 7—8 月；留出期只读取一次。
- 5、15、60 分钟完成柱；信号结束后延迟两分钟，以真实 1 分钟开盘成交。

## 预声明模型

- B0 自身对数价格中枢；PEER 等权其他 19 个标的；MEDIAN 为留一中位数信号配等权执行篮子。
- Ridge、PCA1/3/5 都只用过去 28 天收益，目标标的从因子拟合中排除；AR1 是固定 7 天窗口、4 小时预测控制。
- 中枢使用简单滚动均值、EWMA 和滚动中位数控制；尺度使用因果残差的历史标准差。

## 预声明交易规则

- 入场 `|z| >= 2`，最大持仓 4 小时，组合止损 3%。
- `band` 回到 `|z| <= 0.5` 平仓；`wide_exit` 使用 1；`confirm2` 连续两根确认；`turn` 要求偏离开始回落；`zero_cross` 穿越零点平仓。
- 67 个去重配置全部报告；不因留出结果删除负结果。
- 主网格为 2 bp gross action turnover；固定控制再测 0、1、2、3 bp。

## 审计要求

- 所有滚动中心、尺度、回归、PCA、对冲权重只能使用当前信号柱之前的数据。
- 资金费结算先于同刻成交；净订单和 gross action 手续费同时记录。
- 交易逐笔损益、资金费和权益曲线必须对账；测试和编译检查必须通过。
- 留出期仅用于一次性报告；4 日区块 bootstrap 只作描述性不确定性诊断。
