# 回测净值曲线审计更正

原报告中的 `results/equity_drawdown.png` 为什么会持续向下，原因不是单一的市场表现，而是成本和账本实现共同造成的。

在原始 7 bp 结果中，每个模型的单笔名义金额都固定为 20,000 USDT。代码在 `backtest` 中把 `equity` 固定为初始 100,000 USDT，亏损后没有缩小仓位，也没有在权益耗尽后停止开仓。7 bp 是单边成本，因此开仓和平仓合计约 14 bp，单笔约 28 USDT。原始成交统计如下：

| 模型 | 交易数 | 毛价格 PnL | 成本 | 原始净结果 |
| --- | ---: | ---: | ---: | ---: |
| B0 | 2,547 | +18,631 USDT | 71,329 USDT | −52,698 USDT |
| B1 | 3,419 | −5,440 USDT | 95,738 USDT | −101,178 USDT |
| B2 | 2,892 | +2,104 USDT | 80,980 USDT | −78,876 USDT |

所以费用以近似固定的速度累积，图形看起来接近直线。成本敏感性图本来也是固定交易路径下的线性扣减，不能解读为新的时间序列证据。

审计还发现三个会影响结论的回测问题：

1. B1 的偏离定义是“目标币收益减去市场收益”，但入场又统一乘以 `-sign(z)`，正偏离时实际买入目标币、卖出市场，方向与均值回归相反。
2. 信号时刻立即读取未来 `t+6` 的成交价，并在信号标签时刻计入成交后的浮动盈亏，存在时间顺序错误。
3. 共享对冲腿按每个仓位分别计算绝对成交额，没有在同一执行时刻按每个币的净数量合并后收费。

因此，原始净值图和报告中的收益数字只能作为发现实现问题的诊断结果，不能作为策略有效性结论。此前的“成本后无优势”和“PCA 没有增益”需要在修复事件账本、真实净订单、入场方向和执行时序后重新计算。

本审计由以下检查得到：读取 `experiment_summary.csv` 与三组 7 bp 成交明细，核对每笔名义、费用、成交时间；并用固定价格的合成数据验证了原实现会在 `t+6` 成交前把未来成交价反映到权益曲线中。当前仓库没有把未经完整修复的中间回测结果推送为新结论。
